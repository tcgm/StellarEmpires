{
  "name": "stellarempires",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prepare": "node prepare.js",
    "start": "npm run prepare && cd ./stellar-empires && npm start",
    "build": "npm run prepare && cd ./stellar-empires && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "csv-parse": "^6.0.0"
  },
  "devDependencies": {
    "fs-extra": "^11.3.0"
  }
}
